# Copy to .env (never commit real secrets). Coolify stores these in its UI per-app.
PORT=3000
NODE_ENV=production
APP_ENV=coolify-lab
# COOLIFY_BUILD is set automatically by Coolify at build time (optional).
